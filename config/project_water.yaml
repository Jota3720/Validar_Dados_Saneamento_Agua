database_config: config/database.yaml
layers_mapping: config/layers_mapping_water.yaml
tolerances: config/tolerancias.yaml
rules_catalog: config/rules_catalog.yaml
outputs_root: outputs
domain: AGUA
code_prefix: AGUA
exports:
  driver: GPKG
  overwrite: true
validation:
  require_projected_crs: true
  stop_on_crs_error: true
  active_status_values:
    - ATIVO
    - ACTIVO
    - EM SERVICO
    - EM SERVIÇO
  inactive_status_values:
    - INATIVO
    - INACTIVO
    - ABANDONADO
    - DESATIVADO
    - DESACTIVADO
